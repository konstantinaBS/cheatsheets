# Improve your workflow with zsh

Install zsh

`apt install zsh`

Install oh-my-zsh

`sh -c "$(wget https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)"`

Set default shell to zsh

??

Install plugins

???
